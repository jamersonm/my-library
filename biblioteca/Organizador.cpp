#include <iostream>
#include <list>
#include <iterator>
#include "Biblioteca.h"

using namespace std;

void Organizador::ordem_numerica(string ordem)
{
    if(ordem == "crescente")
    {

    }
    else if(ordem == "decrescente")
    {

    }
}

void Organizador::ordem_alfabetica(string atributo)
{
    if(atributo == "titulo")
    {

    }
    else if(atributo == "nome_autor")
    {

    }
    else if(atributo == "genero")
    {

    }
    else if(atributo == "nacionalidade")
    {

    }
    else if(atributo == "idioma")
    {

    }
}